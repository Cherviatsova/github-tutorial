const magicBtn = document.querySelector('.js-magic-btn');

const navEl = document.querySelector('.site-nav');
console.log(navEl);

const navAllEl = document.querySelectorAll('.site-nav__link')
console.log('navAllEl', navAllEl);


// magicBtn.addEventListener('click', () => {
//     const navEl = document.querySelector('.site-nav');
//     console.log(navEl);

//     const navAllEl = document.querySelectorAll('.site-nav__link')
//     console.log('navAllEl', navAllEl);
// });

// В таком случае тот же код выполнится, но только когда кликать на кнопку. Здесь весь код внутри функции//